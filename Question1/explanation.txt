Command: cd ~/Staff-Graded-Assignment-2/Question1
Explanation: The cd command is used to change the current working directory to the Question1 folder.

Command: mkdir submissions backup
Explanation: The mkdir command is used to create folders that are used to store the original submissions and the backup files.

Command: cd submissions
Explanation: It is used to change the current working directory into the submissions directory to create sample files.

Command: echo "Assignment A" > student1.txt
Explanation: Here we created a sample submission file using the echo command and redirected the output into it.
We did the same thing for all the other student files.

Command: cd ..
Explanation: It is used to return to the Question1 directory.

Command: vi duplicate_backup.sh
Explanation: It is used to create the shell script using the vi editor.

Command: chmod +x duplicate_backup.sh
Explanation: The chmod +x command is used to make the shell script executable.

Command: ./duplicate_backup.sh
Explanation: We are executing the shell script to identify duplicates, back up unique files, generate a report and store the errors separately if there are any errors.

Command: cat report.txt
Explanation: The cat command displayed the report generated by the shell script.

Command: ls backup
Explanation: The ls command lists the files that have been copied into the backup directory.

Command: cat errors.log
Explanation: The cat command is used to display the contents of the error log.
